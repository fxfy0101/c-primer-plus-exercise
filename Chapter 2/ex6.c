#include <stdio.h>

int main(void)
{
    int toes = 10;

    printf("The number of toes is %d.\n", toes);
    printf("The twice of toes is %d.\n", toes * 2);
    printf("The square of toes is %d.\n", toes * toes);

    return 0;
}